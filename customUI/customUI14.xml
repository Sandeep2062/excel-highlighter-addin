<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<customUI xmlns="http://schemas.microsoft.com/office/2009/07/customui"
          onLoad="RibbonCallbacks.onLoad">
  <ribbon>
    <tabs>
      <tab id="tabHighlighter" label="Highlighter" insertAfterMso="TabHome">

        <!-- ================= Controls group ================= -->
        <group id="grpControls" label="Controls">

          <toggleButton id="btnToggleHighlight"
                        size="large"
                        imageMso="ConditionalFormattingMenu"
                        getLabel="GetToggle_Label"
                        screentip="Enable or disable the crosshair highlighter (Ctrl+Shift+H)"
                        supertip="Turns highlighting on or off. Shortcut: Ctrl+Shift+H. Right-click any cell and pick 'Toggle Highlighter' for the same action. By default (This Workbook) it affects only the workbook you toggle it in; switch the scope button in Options to apply it to every open workbook. Your cell formatting is never modified - this only toggles a temporary overlay."
                        getPressed="GetToggle_Pressed"
                        onAction="OnToggle_Action"/>

          <toggleButton id="btnModeRow"
                        size="large"
                        label="Row"
                        imageMso="DeleteRows"
                        screentip="Highlight active row"
                        supertip="Highlights the entire row containing the active cell."
                        getPressed="GetMode_Pressed"
                        getEnabled="GetMode_Enabled"
                        onAction="OnMode_Action"/>

          <toggleButton id="btnModeColumn"
                        size="large"
                        label="Column"
                        imageMso="DeleteColumns"
                        screentip="Highlight active column"
                        supertip="Highlights the entire column containing the active cell."
                        getPressed="GetMode_Pressed"
                        getEnabled="GetMode_Enabled"
                        onAction="OnMode_Action"/>

          <toggleButton id="btnModeCrosshair"
                        size="large"
                        label="Crosshair"
                        imageMso="ViewGridlines"
                        screentip="Highlight row and column"
                        supertip="Highlights both the row and the column of the active cell at the same time."
                        getPressed="GetMode_Pressed"
                        getEnabled="GetMode_Enabled"
                        onAction="OnMode_Action"/>

          <toggleButton id="btnModeCell"
                        size="large"
                        label="Cell"
                        imageMso="ConditionalFormattingMenu"
                        screentip="Highlight active cell only"
                        supertip="Highlights only the active cell or merged range without row or column crosshairs."
                        getPressed="GetMode_Pressed"
                        getEnabled="GetMode_Enabled"
                        onAction="OnMode_Action"/>

        </group>

        <!-- ================= Appearance group ================= -->
        <group id="grpAppearance" label="Appearance">

          <gallery id="galColour"
                   label="Colour"
                   size="large"
                   columns="4"
                   rows="3"
                   itemWidth="32"
                   itemHeight="32"
                   getEnabled="GetGallery_Enabled"
                   getSelectedItemID="GetGallery_SelectedItemID"
                   getItemCount="GetGallery_ItemCount"
                   getItemID="GetGallery_ItemID"
                   getItemLabel="GetGallery_ItemLabel"
                   getItemImage="GetGallery_ItemImage"
                   getItemScreentip="GetGallery_ItemScreentip"
                   onAction="OnGallery_Action"
                   screentip="Highlight colour"
                   supertip="Choose the colour used for the highlight overlay."/>

          <button id="btnCustomColour"
                  label="Custom Colour..."
                  size="large"
                  getImage="GetSwatchImage"
                  getEnabled="GetGallery_Enabled"
                  screentip="Pick a custom colour"
                  supertip="Opens the standard Excel colour picker so you can choose any RGB colour for the highlight. The button shows the current highlight colour."
                  onAction="OnCustomColour_Action"/>

          <toggleButton id="btnPerModeColours"
                        size="large"
                        label="Per-Mode Colours"
                        imageMso="DeleteRows"
                        screentip="Use different colours for row and column"
                        supertip="When enabled in Crosshair mode, the row and column highlights can use different colours. Set them via the Row Colour and Column Colour galleries."
                        getPressed="GetPerModeColours_Pressed"
                        getEnabled="GetPerModeColours_Enabled"
                        onAction="OnPerModeColours_Action"/>

          <gallery id="galRowColour"
                   label="Row Colour"
                   size="normal"
                   columns="4"
                   rows="2"
                   itemWidth="32"
                   itemHeight="32"
                   getEnabled="GetRowColourGallery_Enabled"
                   getSelectedItemID="GetRowColour_SelectedItemID"
                   getItemCount="GetGallery_ItemCount"
                   getItemID="GetGallery_ItemID"
                   getItemLabel="GetGallery_ItemLabel"
                   getItemImage="GetGallery_ItemImage"
                   getItemScreentip="GetGallery_ItemScreentip"
                   onAction="OnRowColour_Action"
                   screentip="Row highlight colour"
                   supertip="Choose the colour used for row highlighting when per-mode colours are enabled."/>

          <gallery id="galColColour"
                   label="Col Colour"
                   size="normal"
                   columns="4"
                   rows="2"
                   itemWidth="32"
                   itemHeight="32"
                   getEnabled="GetRowColourGallery_Enabled"
                   getSelectedItemID="GetColColour_SelectedItemID"
                   getItemCount="GetGallery_ItemCount"
                   getItemID="GetGallery_ItemID"
                   getItemLabel="GetGallery_ItemLabel"
                   getItemImage="GetGallery_ItemImage"
                   getItemScreentip="GetGallery_ItemScreentip"
                   onAction="OnColColour_Action"
                   screentip="Column highlight colour"
                   supertip="Choose the colour used for column highlighting when per-mode colours are enabled."/>

        </group>

        <!-- ================= Options group ================= -->
        <group id="grpOptions" label="Options">

          <toggleButton id="btnExcludeWorkbook"
                        size="large"
                        imageMso="DeleteTable"
                        screentip="Exclude or include the current workbook"
                        supertip="When excluded, the current workbook is skipped by the highlighter entirely. The exclusion travels with the file itself via a hidden defined name."
                        getPressed="GetExclude_Pressed"
                        getEnabled="GetExclude_Enabled"
                        getLabel="GetExclude_Label"
                        onAction="OnExclude_Action"/>

          <toggleButton id="btnExcludeSheet"
                        size="large"
                        imageMso="DeleteRows"
                        screentip="Exclude or include the current worksheet"
                        supertip="When excluded, the current worksheet is skipped by the highlighter while other sheets in the workbook continue to work normally. The exclusion travels with the file via a sheet-scoped defined name."
                        getPressed="GetExcludeSheet_Pressed"
                        getEnabled="GetExcludeSheet_Enabled"
                        getLabel="GetExcludeSheet_Label"
                        onAction="OnExcludeSheet_Action"/>

          <toggleButton id="btnScopeAll"
                        size="large"
                        imageMso="ViewGridlines"
                        getLabel="GetScopeAll_Label"
                        screentip="Apply the toggle to this workbook or to every open workbook"
                        supertip="This Workbook (default): the highlight toggle affects only the workbook you are in. All Workbooks: toggling the highlighter on turns it on for every open workbook at once, and turning it off from any workbook turns it off for all of them."
                        getPressed="GetScopeAll_Pressed"
                        onAction="OnScopeAll_Action"/>

          <toggleButton id="btnAllowProtected"
                        size="large"
                        label="Allow Protected"
                        imageMso="DeleteColumns"
                        screentip="Allow highlighting on protected sheets"
                        supertip="When enabled, temporarily unprotects sheets to apply conditional formatting. Requires explicit consent - protection settings are restored afterwards."
                        getPressed="GetAllowProtected_Pressed"
                        getEnabled="GetAllowProtected_Enabled"
                        onAction="OnAllowProtected_Action"/>

          <toggleButton id="btnDarkMode"
                        size="large"
                        label="Dark Mode"
                        imageMso="FillEffects"
                        screentip="Dark mode tinting"
                        supertip="Adjusts highlight colors to produce pleasant contrast on dark Office themes."
                        getPressed="GetDarkMode_Pressed"
                        getEnabled="GetDarkMode_Enabled"
                        onAction="OnDarkMode_Action"/>

          <button id="btnResetSettings"
                  label="Reset Settings"
                  size="large"
                  imageMso="RefreshAll"
                  screentip="Reset to defaults"
                  supertip="Restores the highlighter to its factory defaults: off, row mode, yellow highlight, per-workbook scope."
                  onAction="OnReset_Action"/>

        </group>

        <!-- ================= Style group ================= -->
        <group id="grpStyle" label="Style">

          <toggleButton id="btnStyleFill"
                        size="large"
                        label="Fill"
                        imageMso="ViewGridlines"
                        screentip="Highlight with cell fill"
                        supertip="Uses cell interior colour for the highlight."
                        getPressed="GetStyle_Pressed"
                        getEnabled="GetStyle_Enabled"
                        onAction="OnStyle_Action"/>

          <toggleButton id="btnStyleBorder"
                        size="large"
                        label="Border"
                        imageMso="ColorPalette"
                        screentip="Highlight with cell border"
                        supertip="Uses a thick cell border for the highlight instead of fill."
                        getPressed="GetStyle_Pressed"
                        getEnabled="GetStyle_Enabled"
                        onAction="OnStyle_Action"/>

          <toggleButton id="btnIntersection"
                        size="large"
                        label="Intersect"
                        imageMso="ViewGridlines"
                        screentip="Highlight the intersection cell"
                        supertip="When enabled, the cell at the row/column intersection gets a stronger highlight for emphasis."
                        getPressed="GetIntersection_Pressed"
                        getEnabled="GetIntersection_Enabled"
                        onAction="OnIntersection_Action"/>

          <toggleButton id="btnAnimated"
                        size="large"
                        label="Pulse"
                        imageMso="Undo"
                        screentip="Animated highlight pulse"
                        supertip="When enabled, the highlight briefly pulses when the selection changes. Opt-in because it uses a timer."
                        getPressed="GetAnimated_Pressed"
                        getEnabled="GetAnimated_Enabled"
                        onAction="OnAnimated_Action"/>

        </group>

        <!-- ================= History group ================= -->
        <group id="grpHistory" label="History">

          <button id="btnHistoryBack"
                  size="large"
                  label="Back"
                  imageMso="Undo"
                  screentip="Navigate to previous cell (Ctrl+Shift+Z)"
                  supertip="Goes back to the previously selected cell."
                  getEnabled="GetHistoryBack_Enabled"
                  onAction="OnHistoryBack_Action"/>

          <button id="btnHistoryForward"
                  size="large"
                  label="Forward"
                  imageMso="Redo"
                  screentip="Navigate to next cell (Ctrl+Shift+X)"
                  supertip="Goes forward to the next cell in history."
                  getEnabled="GetHistoryForward_Enabled"
                  onAction="OnHistoryForward_Action"/>

        </group>

        <!-- ================= Profiles group ================= -->
        <group id="grpProfiles" label="Profiles">

          <dynamicMenu id="ddlProfiles"
                       label="Profiles"
                       imageMso="CustomViews"
                       screentip="Highlight profiles"
                       supertip="Switch between named sets of mode, colour, style, and intersection settings."
                       getEnabled="GetProfile_Enabled"
                       getContent="GetProfilesContent"/>

          <button id="btnSaveProfile"
                  size="large"
                  label="Save Profile"
                  imageMso="FileSave"
                  screentip="Save settings as a new profile"
                  supertip="Saves the current mode, colour, style, and intersection settings as a named profile."
                  onAction="OnSaveProfile_Action"/>

        </group>

        <!-- ================= Help group ================= -->
        <group id="grpHelp" label="Help">

          <button id="btnAbout"
                  label="About"
                  size="large"
                  imageMso="Info"
                  screentip="About Excel Highlighter"
                  onAction="OnAbout_Action"/>

        </group>

      </tab>
    </tabs>
  </ribbon>
</customUI>
